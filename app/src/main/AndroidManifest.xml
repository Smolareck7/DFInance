<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.dfinance">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".addMoney"
            android:label="@string/addM" />
        <activity
            android:name=".cleanMoney"
            android:label="@string/cleanM" />
        <activity
            android:name=".changeList"
            android:label="@string/changeL" />
        <activity
            android:name=".dayOtchet"
            android:label="@string/dayO" />
        <activity
            android:name=".pOtchet"
            android:label="@string/pO" />
        <activity
            android:name=".settings"
            android:label="@string/settings" ></activity>
    </application>

</manifest>